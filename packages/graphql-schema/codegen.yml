overwrite: true
schema: "src/schema/schema.graphql"
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
      scalars:
        DateTime: string
      enumsAsTypes: true
      constEnums: true
      skipTypename: false